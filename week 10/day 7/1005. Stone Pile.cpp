#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    int sum=0;
    for(int i=0;i<n;i++)
    {
        cin>>w[i];
        sum+=w[i];
    }
    for(int j=0; j<(1<<n);j++)
    {
        ss=0;
        for(int i=0;i<n;i++)
        {
            if(j(1<<))
            {
                ss+=w[i];
            }
        }
        trs=abs(2*ss-sum);
        if(rs=trs)
            rs=trs;

    }
    cout<<rs;
}
